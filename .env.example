# ======== AWS OpenSearch Connection ========

# OpenSearch domain endpoint
# Get this from AWS OpenSearch console under "Domain endpoint"
OPENSEARCH_HOST="your-domain.region.on.aws"

# Default HTTPS port
OPENSEARCH_PORT="443"

# Basic authentication credentials (username:password)
# Create a master user in AWS OpenSearch console
OPENSEARCH_BASIC_AUTH="username:password"

# Index name for storing movie embeddings
OPENSEARCH_INDEX_NAME="movies"

# AWS region where your OpenSearch domain is hosted
OPENSEARCH_REGION="us-east-1"
